{# templates/partials/_flash_messages.html.twig #}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }}"> {# Utilisez des classes CSS appropriées (ex: alert-success, alert-danger) #}
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}