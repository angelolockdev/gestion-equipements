<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion de Matériel{% endblock %}</title>
    <link href="[https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css](https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css)" rel="stylesheet">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22[http://www.w3.org/2000/svg%22](http://www.w3.org/2000/svg%22) viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚙️</text></svg>">
</head>
<body class="bg-gray-100 font-sans leading-normal tracking-normal">
    <nav class="bg-teal-600 p-4 text-white shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <a href="{{ path('app_equipment_index') }}" class="text-2xl font-bold">Gestion de Matériel</a>
            <div>
                <a href="{{ path('app_equipment_index') }}" class="px-4 py-2 rounded hover:bg-teal-700">Équipements</a>
                <a href="{{ path('app_employee_index') }}" class="px-4 py-2 rounded hover:bg-teal-700">Employés</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto mt-8 p-4">
        {% for message in app.flashes('success') %}
            <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
                <strong class="font-bold">Succès!</strong>
                <span class="block sm:inline">{{ message }}</span>
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
                <strong class="font-bold">Erreur!</strong>
                <span class="block sm:inline">{{ message }}</span>
            </div>
        {% endfor %}

        {% block body %}{% endblock %}
    </div>
</body>
</html>