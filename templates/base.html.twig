<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion de Matériel{% endblock %}</title>

    {# CSS de Bootstrap 5 via CDN #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    {# Votre CSS personnalisé (optionnel) - Si vous avez un fichier public/css/custom.css #}
    {# <link rel="stylesheet" href="{{ asset('css/custom.css') }}"> #}

    {% block stylesheets %}{% endblock %}

</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_equipment_index') }}">Gestion Matériel</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto"> {# ms-auto aligne à droite #}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_equipment_index') }}">Équipements</a> {# <-- ICI #}
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_employee_index') }}">Employés</a> {# <-- ET ICI #}
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container"> {# La classe container de Bootstrap #}
        {% include 'partials/_flash_messages.html.twig' %}
        {% block body %}{% endblock %}
    </main>

    {# JavaScript de Bootstrap 5 via CDN (doit être après le body) #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    {# <script src="{{ asset('js/custom.js') }}"></script> #}

    {% block javascripts %}
        {# Les scripts spécifiques à une page peuvent être ajoutés ici #}
    {% endblock %}
</body>
</html>